# deprecated camelCase names / hoCort() warns and returns ho_cort()

    Code
      x <- hoCort()
    Condition
      Warning:
      `hoCort()` was deprecated in ggsegHO 2.1.0.
      i Please use `ho_cort()` instead.

# deprecated camelCase names / hoSub() warns and returns ho_sub()

    Code
      x <- hoSub()
    Condition
      Warning:
      `hoSub()` was deprecated in ggsegHO 2.1.0.
      i Please use `ho_sub()` instead.

