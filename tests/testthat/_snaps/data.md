# deprecated atlases / hoCort() warns and returns ho_cort()

    Code
      x <- hoCort()
    Condition
      Warning:
      `hoCort()` was deprecated in ggsegHO 2.1.0.
      i Please use `ho_cort()` instead.

# deprecated atlases / hoSub() warns and returns ho_sub()

    Code
      x <- hoSub()
    Condition
      Warning:
      `hoSub()` was deprecated in ggsegHO 2.1.0.
      i Please use `ho_sub()` instead.

# deprecated atlases / ho2_cereb() warns and returns ho2_sub()

    Code
      x <- ho2_cereb()
    Condition
      Warning:
      `ho2_cereb()` was deprecated in ggsegHO 2.1.0.
      i Please use `ho2_sub()` instead.
      i The cerebellar grey and white matter are structures of `ho2_sub()`.

